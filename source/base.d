module source.base;

immutable mouse_circle_size = 60;
immutable num_of_chain_objects = 100;
immutable num_of_cuddle_objects = 200;
immutable move_step = 5;
immutable size_step = 3;
immutable size_max = 10;
immutable size_min = size_step;
int globel_size = size_min - 1;
bool bigger = true;
int gap_distance = 7;
